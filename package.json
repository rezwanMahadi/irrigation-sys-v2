{
  "name": "irrigation-control-server",
  "version": "1.0.0",
  "description": "Socket.IO server for irrigation control system",
  "main": "heroku-server.js",
  "scripts": {
    "start": "npx prisma generate && node heroku-server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npx prisma generate"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "pg": "^8.11.3",
    "prisma": "^6.10.1",
    "socket.io": "^4.8.1"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
